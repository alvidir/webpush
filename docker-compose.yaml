version: '3.8'

services:  
    ### BACKEND ###        
    webpush-notifier:
        container_name: webpush-notifier
        image: alvidir/webpush:1.0.0-notifier
        restart: always
        ports:
            - 8080:8080
        env_file:
            - .env